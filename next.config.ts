import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  
  images: {
    domains: ["i.pravatar.cc", "i.imgur.com", "images.unsplash.com", "img.icons8.com", "news-images.weatherzone.com.au"],
  }
};

export default nextConfig;
